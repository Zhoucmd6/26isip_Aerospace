# 2026-09-03 X8PHYS 校准数据采集清单

项目组：周航正、霍奕茗、于跃、叶安、王健祺
文件负责人：待组内确认
本次贡献：霍奕茗（提出完整数据采集与配置位置核对需求）；Codex（参数审计、数据矩阵和配置映射整理）
审核：待项目组审核、待指导教师确认
AI协助：Codex

## 本次做了什么

- 新增 `models/px4_x8/data/X8PHYS_CALIBRATION_PLAN.md`，逐项列出几何/惯量、单桨静推、同轴干扰、执行动态、非零来流、机身气动、电池、附属功耗、Plane 联合功率、测量链和安全门槛所需数据。
- 为每类数据标明最低原始字段、工况覆盖、现有 `x8phys.config` / `plane.config` 配置字段和当前缺失入口。
- 在 X8PHYS、Plane 和数据追溯入口增加交叉链接，区分“采集计划”和“已核验事实”。
- 重申三个仓库根目录受限源文件不得通过移动、复制、改名或解除忽略规则加入 Git。

## 关键决策与理由

- 重心、完整旋翼三维位置、同轴干扰、动态 `Ct/Cp(J)`、热模型和 MeasurementAdapter 目前没有正式配置字段，按“未实现”登记，不能把数据硬塞进语义相近的标量字段。
- Plane 与 X8PHYS 当前电池、质量和功率 proxy 不一致；正式校准必须建立单一参数来源，不能长期双重维护。
- calibration/holdout 按完整试验 run 或硬件批次拆分，避免同一时间序列相邻样本泄漏。

## 遗留问题 / 风险

- 参数文件 schema 和装载器尚未建立；本次只补齐采集与配置计划，不改变运行模型或证据等级。
- 数据提供者仍需确认 `models/plane/data/raw/功率-升力.xlsx` 的字段、单位、仪器和试验条件。

## 下一步

- 由项目组确认负责人、硬件清单、真实电池 S/P 和试验资源，再按清单确定首批台架 run ID。
- 参数装载器和新增物理字段应单独设计接口与负向测试，完成留出验证后再替换 proxy 默认值。

## 验收状态

- 文档治理检查：通过（15 个模块、27 个活动 Markdown 文件）。
- X8PHYS/Plane 数值验收：本次未改运行代码，不要求重跑；现有 PR #1 验收结论保持不变。
