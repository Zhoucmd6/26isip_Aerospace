# 3.6 platform_link 证据：接手修复后正式批（2026-09-09）

状态：**正本，已关闭**（2026-09-09 接手修复正式批 52/52+20/20；**人工验收 H1–H5 叶安确认通过（2026-09-09 "过了"）**，含图1样张勘误后重出——首版图1画图脚本误用 polyval 降幂解释仓库升幂拟合系数，正确曲线谷底 5.48，算法与门槛数字不受影响；3.6 任务关闭）
方案：[`docs/T36_PLATFORM_LINK_TAKEOVER_PLAN.md`](../T36_PLATFORM_LINK_TAKEOVER_PLAN.md)（2026-09-09 叶安拍板 D1–D3）
负责人：王健祺（3.6 原创 + WIP 交接）/ 叶安（接手修复）

## 范围与结论

算法线 3.6 平台对接修复后正式批：**单元测试 52/52（含新增 M1 查询时间语义、M5 风估计物理界两个回归锚），检查门槛 20/20（平台五门原注册零增改全过）**。修复缺陷 F1–F5（方案 §2，含实测锚定）；F6 证据矛盾由本次重出消除；F7 模块登记已补（2026-09-09，治理 PASS）。

## 接手修复内容（全部限定 modules/platform_link/）

| # | 修复 | 位置 |
|---|---|---|
| F1 | `q()` 每次查询推进恰 1.0 s（原误传内环步数 100 → 每查询 100 s，标定 150 样本需 ≥15000 s ≫ 900 s 预算，sweepcal 拟合从未执行、û\* 停在初值 7.5） | `+w36/make_platform_plant.m` |
| F4 | 平台后端 `rng(c.seed)`（原用全局 RNG 状态，结果不可复现） | 同上 |
| F5 | 环境探测 `exist(...,'file')`→`which(...)`（R2022b 包函数恒 0）、回退路径三级回溯、回退整拷 `models/plane`（含 `data/calibration`） | 同上 |
| F2 | Phase B 风修正三道守卫：守卫 A 风估计物理界 ±8 m/s；守卫 B 覆盖门（航向净转角 ≥0.8 rad 且指令速度极差 ≥1.0 m/s 才执行 wind_corr）；守卫 C disc<0 保持上一条可行指令（原 `max(q,0)` 曾使 Phase B 平均地速 0.53 m/s） | `+w36/sweepcal_run.m` |

算法策略机器（标定/重拟合/探针/白名单红线 1/2）零改动；本地后端路径零改动（3.5 数字与 pretakeover 报告一致，见横比表）。

## 运行协议与确定性

- 工作树冻结，`run_task36_checks` 正式跑两遍（2026-09-09 12:15 / 12:27），**三份 CSV 逐位相同**（main_comparison / platform_link / wind_kinds_smoke）。
- 如实记录：第 2 遍首次尝试在单测 52/52 通过后遭遇 MATLAB 原生堆崩溃（0xc0000374，运行时层，M3 第三轮复验历史有同类记录），重跑完整通过；崩溃前无部分结果进入证据。

## 平台五门实测（seed 11，composite B=2.5/σ=0.3，900 s）

| 门 | 实测 | 判据 |
|---|---|---|
| 逐秒日志覆盖 | rows=900 | ∈[900,1125) |
| sweepcal 盲找回谷底 | û\*=5.0765（标定瞬间 5.6712，真值 5.15） | \|û\*−5.0\|<1.2 |
| sweepcal 尾段 | regret 2.98%（超额 8.14%） | <8% |
| known oracle（零风） | −0.40% | <3% |
| purerl_off 优于开环 | 4.71% vs 5.61%（余量 0.90 pp） | 同后端内部对比 |

确定性锚：平台 sweepcal 独立复现与正式批逐位一致（超额 8.13767%、尾段 2.9807%）。

## 归档与代码状态绑定

- `*_pretakeover_20260908.*`：王健祺 WIP 提交 `d3f7966` 状态的旧证据。其 report 曾写 20/20 全过，但该代码状态下 F1 使标定在 900 s 配置内数学上不可能完成，全过结论不可能出自该状态（诊断记录见方案 §2 F6）；保留仅作历史，不作为结论依据。
- 本目录正本四件（report.md + 三 CSV）生成于修复后提交状态，运行日志（FORMAL RUN1/RUN2 摘要）见 `docs/worklog/2026-09-09-zcode-t36-takeover.md`。

## 边界

- 全部结论锁 V1 桌面仿真代理口径（AGENTS.md 红线 3），不支持真实 X8 节能表述；known 为非因果 oracle 参照。
- 平台主线（models/plane、harness、M3 寻优链、全部 .slx）零改动；顶层路线图 M4 与"RL 仅 M4 后进平台"条款不受影响。
- 就位语义深化（方案 D3/条件阶段 5）未触发——五门在守卫生效下全部过带。

## 追加（2026-09-10）：平台数据源对等性修复 + 10 倍窗验收（王健祺/ZCode）

- 触发：D3 条件阶段 5（就位语义深化）+ 数据源一致性专项（用户报告平台后端拟合出界/恒速飞行）。
- 修复 N1–N8：q() 功率归一化补漏、就位委托制、RL 更新门放行、purerl 预训练平台数据源+缓存模型、fit_curve_wind 帧坍缩抗退化、扫频 [2,13.5] 与探针 100、demo 平台瓦数显示、部署路径多候选。
- 验收（9000s、B=2.5 默认复合风、seed=11、单种子）：openloop 4.48% | purerl_off 0.54% | purerl_on 1.60% | sweepcal 1.80% —— 全部优于开环；B=3.5 sweepcal 5.02%（修复前 25%）。
- 证据文件：acceptance_9000s_20260910/（5 臂 CSV + sw100/sw150 学费分解日志）；详细发现与平台困扰清单见 docs/T36_PLATFORM_PARITY_FINDINGS_20260910.md。
