# 共轴八旋翼在线功率优化：执行路线与验收门槛

版本：2026-09-04
状态：**本文件是后续实现、试验和评审的唯一执行基线。**如算法方向调整，应先更新本文件的接口与验收条件，再改模型或代码。

项目组：周航正、霍奕茗、于跃、叶安、王健祺
文件负责人：叶安
主要撰写：叶安（阶段划分、验收门槛与放行决策）、ZCode（历轮方案文档与状态回填代拟）
技术贡献：Codex（M0-B 复核口径 4.1 统一、Wind-Plane-Control 架构衔接、Plane 工作包及修正版研究路线整理，2026-09-01～09-03）
本次修订：周航正（提出 Plane 物理建模任务和名义功率图/在线寻优/接口草图澄清需求，2026-09-02～09-03）；Codex（静推/电芯数据接入与 P0--P4 验收路径，2026-09-03）；周航正（2026-09-04 提出"Agent 产出如何人工验证、飞机/控制/环境三线如何集成、models 最新任务如何做出演示展示"需求）；ZCode（§0.6 四步递进验证流程、§0.7 演示与展示层工作包与 §4/§6 状态同步代拟，2026-09-04）
审核：待项目组审核、待指导教师确认
AI协助：ZCode（本基线的持续维护代理）、Codex（路线与状态整理；2026-09-04 按叶安要求回填 M3 第二轮独立验收，不改变冻结门槛）；重大条目均待负责人确认后生效

文档关系：任务目标与范围由 [`architecture/01_problem_definition.md`](architecture/01_problem_definition.md) 定义，公共字段语义由 [`architecture/04_interface_dictionary.md`](architecture/04_interface_dictionary.md) 定义，本文件只管理执行阶段、汇合点和验收放行；具体阶段接线在 `interfaces/M*.md` 中展开。发生冲突时先修正对应的上游权威文档，再同步下游实现说明。

## 0. 2026-09-03修正版研究路线

### 0.1 先统一研究对象

无风仿真扫描得到的 `P_nom(v_air,eta)` 是名义模型，不是在线算法要重新“发现”的唯一答案。在线策略面对的运行对象允许相对名义模型存在风、转弯、执行动态、电池和参数偏差：

```text
P_hidden = F(v_air, eta, turn, battery, disturbance; hidden_parameters)
P_meas   = delayed_noisy_measurement(P_hidden)
```

第一阶段优化变量是沿航迹切向地速参考 `v_ref`，第二阶段加入上下桨转速比参考 `eta_ref`。主目标仍为固定任务时域内平均电功率最小，同时满足轨迹、姿态、速度和执行器约束。名义模型、隐藏对象和在线测量的信息边界见 [`decisions/ADR-004-power-map-information-boundary.md`](decisions/ADR-004-power-map-information-boundary.md)。

### 0.2 算法开发线

| 工作包 | 需要完成什么 | 当前可复用资产 | 当前状态 |
|---|---|---|---|
| ALG-A 接口适配 | 将固定值、名义调度、速度ESC统一为 `reset/step(MeasuredContext)->ControlCommand` | `speed_esc`、`wind_field_sched` | 模块各自可运行，统一适配未完成 |
| ALG-B 因果风场决策 | `known`真风策略只作Oracle；正式策略只能用当前/历史风测量或功率反馈 | `wind_field_sched`在线/盲策略、风场搜索模块 | 有代理证据，尚未接统一接口 |
| ALG-C 速度与转速比协同 | 先交替坐标下降：一次只更新 `v_ref` 或 `eta_ref`，共用稳定窗和安全门控 | `speed_esc`、`ratio_esc`、平台M2 | 代理对象已有机制证据；M3 第二轮独立验收 F2/F3/F4 已由第三轮修复关闭（修复方复验，见 §5），待独立复验；Plane 复跑（R4）待就绪 |
| ALG-D 模型不匹配评价 | 在多个未见 `P_hidden` 参数集上比较固定、名义调度和ESC，不按单一曲线调答案 | 多峰/平移/自适应模块；`realistic_constraints_search`、`curve_case_calibration`、`wind_model_library` | 任务7–9已补充真实约束、文献曲线case和风模型库；其中因果策略在部分真实约束代理中不敌开环，曲线参数仍未校准，需先统一Plane与信息边界后再形成主结论 |
| ALG-E 残差RL | 仅在强基线仍有可重复缺口时，用 `v_ref=guard(v_base+delta_v_RL)` 学习残差 | 两个RL预研模块 | 保留预研，暂不进入平台主线 |

算法线不负责生成风、计算飞机真实空速/功率或跟踪姿态，也不得直接输出PWM。

### 0.3 Plane无人机建模线

| 工作包 | 需要完成什么 | 验收重点 |
|---|---|---|
| PL-A 最小Plane API | 实现统一 `reset/step`，接收WindTruth和AppliedCommand，输出PlaneState | 与接口0.3字段、单位、时间戳一致 |
| PL-B 运动与执行动态 | 地速响应、空地速换算、直线/圆周运动、`v_ref/eta_ref`限速与饱和 | 零风恒等、顺逆风符号、阶跃和圆周 `v^2/R` |
| PL-C 功率对象分层 | 建立公开 `P_nom(v_air,eta)`；另建参数化隐藏变体 `P_hidden` | 名义切片可复现，隐藏参数不泄露给Control |
| PL-D 能源与测量 | 功率到能量积分、电压/SOC、功率噪声/延迟/缺测及来源标志 | `P_meas`与真值时序可追溯，能量积分一致 |
| PL-E PX4适配 | 将统一Plane/Measurement接口接到 `models/px4_x8`，复用M0-M2安全链 | 旁路回归、字段映射、M0/M2基线不退化 |

Plane线不选择最优速度，也不读取未来场景。真实数据缺失期间所有参数继续标记为 `proxy/estimated`。

### 0.4 Environment与场景建模线

| 工作包 | 需要完成什么 | 当前可复用资产 | 当前状态 |
|---|---|---|---|
| ENV-A PathCommand | 从Wind中拆出直线/圆周几何、相位、切向/法向、目标高度和最低地速 | 圆周/正弦风模块 | 定义完成，实现待统一 |
| ENV-B WindTruth | 输入时间和位置，输出统一NE环境风速度，覆盖无风/恒风/分段/正弦/OU/阵风 | `wind_field_sched`等 | 多套实现存在，符号尚需适配 |
| ENV-C WindMeasurement | 在真值外单独模拟传感器/估计器采样、噪声、延迟、数据年龄和缺测 | RL环境已有部分逻辑 | 尚未形成公共模块 |
| ENV-D 场景与种子清单 | 固定训练、调参、未见测试的场景ID、随机种子、时长和约束 | Harness和各模块场景 | 尚未统一到WPC Harness |

Environment不输出飞机航向、地速、空速或功率。当前 `wind_field_sched` 的局部 `u=v*t+w` 接入时必须按接口字典完成符号适配。

### 0.5 汇合顺序与RL准入

| 里程碑 | 三条线怎样汇合 | 放行条件 |
|---|---|---|
| R0 概念冻结 | 确认主任务、`v_ref`地速语义、名义/隐藏/测量边界 | ADR-001和ADR-004经组内/教师确认；接口字典仍需冻结 |
| R1 Mock闭环 | Environment、Plane、Control各自可被Mock替换 | 接口契约测试和一键日志通过 |
| R2 无风基线 | 同一Plane上比较固定、名义最优和速度ESC | 控制器无隐藏信息，配对评价成立 |
| R3 因果风场 | 加入圆周规则/不规则风，区分Oracle、测量风和盲策略 | 无未来风泄漏，未见场景结果可复现 |
| R4 双变量协同 | 在同一Plane上交替更新 `v_ref/eta_ref` | 不差于两个单变量基线，零硬违规 |
| R5 RL复审 | 只在R0-R4后评估残差RL是否必要 | 满足ADR-002，优于强基线才晋级 |

R1之前三条线可以完全并行：各自只依赖接口0.3和Mock。RL当前只做接口适配和观测/动作审计，不扩大训练；它不是R1-R4的前置条件。

### 0.6 验证与测试总体流程（四步递进验证，2026-09-04 新增）

针对"仓库产出大量由 AI 代理完成、人工难以逐行验证闭环逻辑"的现实约束，全部后续验证按以下四级递进组织。每级只在前一级放行后启动；任何一级不得跳级引用下一级结论，也不得把低级别结果写成高级别结论（与 §2 约束 5/6 同源）。

| 级别 | 名称 | 内容 | 对应仓库里程碑 | 放行条件 |
|---|---|---|---|---|
| V1 | 桌面模型仿真（纯软件） | AI 生成的 Plane 模型、环境模型与算法在统一 Harness 中闭环运行：接口契约测试 + Toy Example + 场景/种子矩阵 | R1--R4、M0--M3、`harness` | 契约与配对验收全绿；同种子一键复现；独立验收会话复核 |
| V2 | 半物理仿真（HIL/SITL） | 真实飞控计算机接入仿真环境，算法与安全层运行在目标硬件或 PX4 SITL 中 | M4 第 3 条 | SITL/HITL 接口与纯 Simulink 接口一致；M0--M3 对比复跑不退化 |
| V3 | 地面物理验证 | 禁飞期间地面架设（倒扣飞机模拟升力、绑缚测试）与台架数据采集，回灌校准 Plane 参数 | M4 第 4/5 条、P 线校准入口 | "台架数据→清洗对齐→参数辨识→仿真校准→留出验证"闭环成立；参数来源从 `proxy` 升级为 `calibrated` |
| V4 | 实飞验证 | 禁飞解除后真实飞行报备与数据采集，复验 V1--V3 结论 | 后续（另行立项） | 飞行日志回放与仿真口径一致；安全链零误触发；结论以实测来源标志发布 |

**复杂系统验证方法论**（贯穿 V1--V4，对应"物理开发底层不可见"的管理手段）：

1. **接口与架构管理**：不逐行审计 AI 代码，而是管住三件事——接口（接口字典 0.3 冻结为 1.0 后任何改动走 ADR）、架构分区（§1 五条工作线互不越界、Mock 可替换）、结果验证（MOP/MOE 配对评价 + 一键日志）。
2. **Toy Example 测试案例**：把场景矩阵中最小、解析可预期的案例固化为标准层级，任何模型/算法改动先过 Toy Example 再进全场景矩阵。建议首批登记：
   - T1 零风直线：`v_air ≡ v_ground`，P(v) 单谷且最优速度解析已知（PL-B 零风恒等门槛的直接演示化）；
   - T2 恒风圆周：`v*(t)=−q+sqrt(q²+V*²−|w|²)` 解析调度可预期（`wind_field_sched` 解析解）；
   - T3 正弦/双正交风：功率曲线平移周期与拍频可预期（任务4/5 口径）；
   - T4 阶跃/边界：`v_ref` 限速、`eta_ref` 饱和与安全回退时序可预期（M0-B/M2 故障注入口径）。
   各模块既有 `tests_*` 已具备雏形，统一以 T1--T4 登记进 `docs/architecture/05_verification_traceability.md`，期望值写死在验收脚本中。
3. **证据治理**：沿用 ADR-003 分层判定与 `ACCEPTANCE_AUTOMATION_RULES.md`；AI 产出一律经独立验收会话（换会话/换执行者）复核后才可放行——M2 十轮与 M3 第一轮 PARTIAL 的既有教训。

V1 是当前全部工作的所在级；V2--V4 在本文件中只冻结接口与放行条件，不预支结论。

### 0.7 演示与展示层（WPC 统一面板，2026-09-04 新增）

`modules` 任务1--9 已沉淀成熟的"三模块面板"范式：左侧三按钮切换**控制台（主）/飞机模型/环境模型**，配 MOP/MOE 结果卡片、日志栏、导出与验收报告载入（最新实现见任务8/9 的 `START_HERE`/`launch_task*_demo`）。`models` 最新任务（Plane P0--P4 统一物理对象）与 harness 汇合闭环目前只有验收脚本、无演示面板。为使 Wind-Plane-Control 集成成果可展示、可汇报（周报/展示材料），新增演示展示层：

| 工作包 | 内容 | 复用资产 | 验收门槛 |
|---|---|---|---|
| UI-A WPC 闭环统一面板 | 以 harness 三模块架构为骨架的三模块面板：控制台=慢层四模式选择（fixed / nominal_sched / ESC / qnewton）+运行控制与状态；飞机模型=速度/功率双表盘+能耗/SOC/`eta_actual`；环境模型=圆周航迹+风矢量箭头+风速曲线（接入任务9 七种风场库） | 任务8/9 面板框架、`harness/run_mop_moe_demo` | 面板只读测量信号（黑箱红线不破）；演示数值与验收脚本同种子一致；qa 面板检查通过 |
| UI-B Plane 物理视图 | 第四视图"Plane 物理对象"：地速/空速风三角分解、`P_nom` 曲面切片与当前工作点、电池 SOC 轨迹、8 位约束标志灯 | `models/plane/+plane/step` 诊断输出 | WindTruth/`P_hidden` 真值通道仅在视图侧显示并与控制台隔离，不进入算法输入 |
| UI-C 汇报导出模式 | 一键导出汇报截图/CSV/动画与里程碑状态视图（R0--R4、V1--V4、M0--M3） | 各模块导出函数、`展示材料/` 现有脚本 | 导出产物带模型/算法版本、种子与场景 ID，可追溯到 `docs/evidence` |
| UI-D 面板验收自动化 | 沿用 `qa_*` 模式为 UI-A/B/C 建立回调、播放/暂停与截图检查 | `qa_ui`/`qa_speed_ui`/`qa_task*_demo` | 纳入 `ACCEPTANCE_AUTOMATION_RULES.md`；面板改动提交前必跑 |

展示层红线：面板不是新的权威数据源，引用数值仍以 `docs/evidence` 为准；面板算法切换不得绕过安全层直接驱动 PWM；演示效果不写入可引用结论。UI-A 依赖 harness↔Plane 对接（§6 第 2 条）产出闭环数据通路，可先用现有代理对象出样张。

## 1. 项目分工与总体闭环

本项目分为五条可并行推进、通过固定接口汇合的工作线：

| 工作线 | 负责对象 | 当前归属 | 产出 |
|---|---|---|---|
| 优化算法线 | ESC、速度与转速比协同策略、后续 RL | Git 仓库协作成员 | 可调用的慢层决策模块 |
| Environment / 场景线 | 任务路径、风真值、风测量退化、场景与种子 | 王健祺已有较多资产，最终归属待组内确认 | `PathCommand`、`WindSample`与场景清单 |
| Control / 飞控平台线 | 速度与姿态快层、X8 控制分配、安全回退、PX4/Simulink 主模型 | 叶安 | 稳定可控的飞行与执行器闭环 |
| Plane / 仿真物理建模线 | 风与空地速换算、阻力/圆周运动、速度与转速比执行动态、联合功率及电池代理 | 建议负责人：霍奕茗（待组内确认） | 可替换、可标定的物理对象与统一状态反馈 |
| Evaluation / 集成线 | 公共适配器、Harness、MOP/MOE与证据 | 周航正统筹，指标资产由王健祺维护 | 公平比较、一键运行和可视化 |

各条线不靠复制同一份模型并行开发。算法线只调用接口；Environment和Plane先作为独立 MATLAB/Simulink 组件开发和验收；Control线继续由叶安维护 `models/px4_x8/*.slx`；Evaluation按接口提供Mock和统一场景。Plane单元通过后，再由Control线负责人按接口接入当前开发模型。

目标闭环如下。优化算法**不直接写八路 PWM**；八路指令由飞控与受约束分配器产生。

```text
Scenario/Path：轨迹、任务时长、约束
Environment：WindTruth / WindMeasurement
        ↓
慢层算法：v_ref、eta_ref、模式
        ↓
接口与安全层：范围检查、冻结、基线回退
        ↓
Control：速度控制 / 姿态飞控 / X8 控制分配
        ↓
Plane：执行动态 / 风与空地速 / X8 动力学 / 联合功率对象
        ↓
地速、空速、姿态、PWM、RPM、功率、能耗、SOC、约束标志
        └────────────────────────────→ 慢层算法
```

真实台架或飞行数据的正确用途是校准仿真对象，而不是直接替代仿真结果：

```text
真实数据 → 清洗/时间对齐 → 参数辨识 → 更新仿真参数
        → 仿真验证算法 → 台架/SITL/飞行复验
```

## 2. 不可违反的架构约束

1. 快层负责稳定：姿态、角速度、解锁、基本限幅与故障保护必须保留。
2. 慢层只输出高层目标：初期为 `v_ref`；M2 起增加 `eta_ref`。不得绕过飞控直接驱动 `PWM1...PWM8`。
3. 同轴比值定义为 `eta = omega_upper / omega_lower`。每一对上下桨的编号、旋向和配对关系必须在接口文档中固定。
4. 每个优化运行都必须可与“固定参考、优化关闭”的基线一一比较。
5. 功率信号必须标明来源：`estimated`（模型估算）或 `measured`（实测/校准）。未经校准的估算功率不能宣称真实节能率。
6. 任何安全条件触发时，优化器冻结并回退至最后安全参考或固定基线；安全层不放在 ESC 内核中。
7. 在没有实机或台架数据前，Plane 线所有功率、阻力、电池和执行器参数均标为 `proxy` 或 `estimated`，不得写成已标定物理量。
8. 世界坐标系统一使用 NE 平面；空速定义为 `v_air_ne = v_ground_ne - wind_ne`。风场由 Wind 模块提供，Plane 负责使用，Control 不重复实现风场或功率公式。

## 3. 统一接口合同

### 3.1 算法输入（来自平台）

| 信号 | 单位 | M0 | M2 起 | 说明 |
|---|---:|---:|---:|---|
| `t` | s | 必需 | 必需 | 仿真或日志时间 |
| `v_ground_ne[2]` | m/s | 可由现有 `v` 兼容 | 必需 | 北、东方向地速；标量 `v` 为沿航迹投影或水平模长，使用处必须注明 |
| `v_air_estimate_ne[2]` | m/s | 可选 | 按信息模式可选 | 空速传感器或MeasurementAdapter估计；不得直接取仿真真值 |
| `wind_measured_ne[2]` | m/s | 可选 | 按信息模式可选 | Wind测量/估计；须带时间戳、年龄和有效性，`measurement_only`策略可不使用 |
| `P_e` | W | 必需 | 必需 | 估算或实测电功率，须带来源标志 |
| `E_e` | J | 必需 | 必需 | `P_e` 时间积分 |
| `soc` | 0--1 | 可选 | Plane 电池阶段起 | 简化电池状态；没有标定前只作代理状态 |
| `attitude` | rad | 必需 | 必需 | roll、pitch、yaw |
| `yaw_rate` | rad/s | 必需 | 必需 | 偏航约束监测 |
| `motor_pwm[8]` | us | 记录 | 必需 | 最终执行器命令 |
| `motor_rpm[8]` | rpm | 记录 | 必需 | 实际或模型估算转速 |
| `constraint_flags` | bool bus | 必需 | 必需 | 饱和、姿态、偏航、功率/电流、数据异常等 |

### 3.2 算法输出（交给平台）

| 信号 | 单位 | 生效阶段 | 含义 |
|---|---:|---|---|
| `v_ref` | m/s | M0 起 | 速度参考，受范围与变化率限制 |
| `eta_ref` | — | M2 起 | 上下桨转速比参考，受范围与变化率限制 |
| `optimizer_enable` | bool | M0 起 | 优化器使能；关闭时走固定基线 |
| `optimizer_status` | enum/string | M0 起 | warmup、search、hold、frozen、fallback 等 |

### 3.3 平台输出与记录

每次运行至少归档：配置、模型版本/哈希、算法版本/参数、初始条件、噪声与时延设置、上述所有接口信号、事件与失败原因。结果目录不得覆盖历史运行。

## 4. 当前所处位置

（2026-09-04 同步）**阶段 0、M0-A/B/C、M1 已完成并通过验收。M2 已完成受约束 PWM 域 eta 分配器、`ratioesc` 原生转速比接线与快照 `air_m2.slx`，核心成果保持放行：第六轮发现的标准矩阵缺口（源码绑定、verdict 硬断言、C5 长链）已由第七至十轮关闭，第十轮在 `71acd56` 按规则 v1.7 分层复跑 52/52 针对性矩阵 PASS，功能实现与验收基础设施层均 VALIDATED（`docs/evidence/M2_REACCEPT_ROUND10_CODEX_20260903.md`）。M3 v/eta 交替协同 B--E 组已实现并保留代理对象机制证据：第一轮独立验收的 F1/F5 已在第二轮关闭；第二轮的 F2/F3/F4/F6 已由第三轮修复关闭（修复方复验，batchId `9b7a6980`，14 臂批次经治理链重跑 PASS），待项目组独立复验通过后 M3 代理阶段方可收口，R4 统一 Plane 复跑继续后置。算法线同步转向：ESC 降级为开发基线与 WPC 四模式之一，任务6 起 qnewton 为新推荐，路线§3-5 交付口径转向名义功率图 + 解析调度 + 在线风估计（见 README"算法路线现状"）。**

已完成（平台线明细）：

- `air.slx` 可更新并完成 0–10 s 仿真；
- 已保存一次基线结果；
- 已完成 Attitude Control、八路 PWM、6DOF 的接口检查；
- 已生成不直接修改原模型的 `air_spare.slx` 副本；
- 已在 `air_spare/Subsystem` 中从 6DOF `Ve` 无侵入分支出完整惯性速度和水平速度日志；修改后仿真通过，水平速度日志覆盖 10001 个样本、0–10 s；
- 已在 `air_spare/Attitude Control/AttitudeControl` 的 8 路 PWM 分解点分支出 `motor_pwm[8]`，并按现有植株 PWM→角速度映射生成 `motor_rpm_est[8]`；修改后仿真通过；
- 已新增可替换 `M0A Power Measurement` 子系统：`P_est = C_M·Σω³`（`C_M=2.51e-7` 与植株扭矩系数一致，来源标志 0=estimated），`E_est` 为其积分；与独立重算最大偏差 5.7e-14 W，10 s 能量一致性相对误差 4e-8；
- 已建立 35 维统一日志总线 `m0a_log_bus` @1 ms（`[v, P_est, E_est, power_source, att(6), pwm(8), rpm_est(8), flags(8), optimizer_enable]`）与 `optimizer_enable=0` 固定基线配置；
- 已定义 8 位约束标志（pwm/rpm 饱和、姿态、偏航率、速度失跟、功率异常、信号缺失、预留；阈值为文档化占位值）；名义运行初始化窗后除速度失跟（占位 `v_ref=0` 语义）外全零；
- **M0-A 验收通过**：`air` 与 `air_spare` 相同 10 s 场景下 `pwm_cmd`/`Ve`/`quat` 逐样本最大绝对差 0（观测层不改变飞行行为）；已另存稳定快照 `air_m0a.slx`。
- **M0-B 验收通过（2026-09-01 修复后复验）**：`air_spare` 装入速度外环与安全层后，旁路模式（`speed_loop_enable=0`）与 `air` 基线逐样本最大绝对差仍为 0；名义场景 9 m/s 均值误差 0.032 m/s，扰动场景 5/9 m/s 均值误差 1.615/1.598 m/s；逐位故障注入链与 13 s 严格恢复通过。已另存稳定快照 `air_m0b.slx`。
- **M0-C 验收通过（2026-09-01）**：`ratio_esc` 内核经 `m0c_vref_esc` 仅输出 `v_ref`，四组 fixed/ESC 配对和一组确定性复现通过；快照 `air_m0c.slx`。当前功率面近平坦，不支持节能结论。
- **M1 验收通过（2026-09-01）**：2% 功率噪声（5 种子）、0.5 s 反馈时延、基线正弦扰动及三种子组合共 27 场景，安全链零误触发，11 组 fixed/ESC 配对 regret 最大 |0.000133%|（≤3% 门槛），DL 确定性差 0，噪声背景故障回归 4/4；全程内存注入零 `.slx` 变更。regret 门槛满足属平坦功率面结果，不构成节能证据。

未完成：统一 Wind-Plane-Control Harness 接线（harness 的 aircraft 代理尚未替换为 `models/plane` 统一对象）、ENV 线 PathCommand/WindTruth/WindMeasurement 统一、M3 项目组独立复验（第二轮 F2/F3/F4/F6 已由第三轮修复关闭、修复方复验）与同一 Plane 复跑（R4）、整机 S/P 与动态参数校准、SITL/HIL（V2）与平台侧 RL 接入、任务7 入口脚本补齐（`START_HERE.m`/`run_task7_acceptance.m` 未入库，见 worklog 2026-09-04）、演示展示层 UI-A--D。Plane P0 拟合门槛与 P1--P4 MATLAB 代理契约已交付，尚未接入同一 PX4 `.slx`。

## 5. 分阶段实施清单

### 阶段 0：模型基线与接口审计（完成）

目的：证明现有模型可运行，并明确可插入优化层的位置。

- [x] 运行 `air.slx` 更新与 10 s 仿真。
- [x] 归档基线和顶层结构。
- [x] 导出关键子系统与 6DOF 的端口连接。
- [x] 创建 `air_spare.slx`，原模型保持不动。

验收：已有成功仿真和归档结果；目前满足。

### M0-A：测量、日志与基线模式

目的：使模型可被观察和可安全比较，不改变现有快层的基本稳定逻辑。

1. [x] 在 `air_spare.slx` 从 6DOF 的速度输出派生 `v`；初期采用 `sqrt(Ve_x^2 + Ve_y^2)`，并在日志注明“无风地速代理”。
2. [x] 从最终 PWM 分解点记录 `motor_pwm[8]`，并按当前植株的 PWM→角速度关系导出 `motor_rpm_est[8]`。后续电机动态/传感器模型加入后，再替换为实际 RPM。
3. [x] 增加 `Power Measurement` 子系统：先输出明确标注为 `P_est` 的估算功率和 `E_est`；接口预留日后替换为电机/ESC/电池或实测功率。（`P_est = C_M·Σω³`，与植株同源；来源标志 0=estimated）
4. [x] 创建统一日志总线和场景配置结构；保留 `optimizer_enable=0` 的固定基线模式。（35 维 `m0a_log_bus` @1 ms；配置归档 `results/m0a_config/`）
5. [x] 定义并记录约束标志：PWM/RPM 饱和、姿态过大、偏航率过大、速度失跟、功率异常、信号缺失。（8 位，阈值占位待 M0-B 校准）

验收：**已通过（2026-08-31）**。基线模式下完成 10 s 仿真；日志可绘制 `v`、8 路 PWM、8 路 RPM、`P_est`、`E_est`、姿态和约束标志；`air` 与 `air_spare` 的 `pwm_cmd`/`Ve`/`quat` 逐样本最大绝对差为 0，观察模块未改变原有飞行轨迹。稳定快照 `air_m0a.slx` 已另存。

### M0-B：速度闭环与安全回退（完成）

目的：建立慢层可安全操纵的速度通道。

1. [x] 参考选择器 `M0B Reference & Safety`：`optimizer_enable=0` 走手动固定 `v_ref`（状态 0），`=1` 走优化器参考（状态 1/2）；范围 [0,15] m/s、变化率 2 m/s²、warm-up 由速率限制实现。
2. [x] `v_ref → 速度控制器 → pitch_cmd` 外环 `M0B Speed Controller`（投影 PI，`Kp`/`Ki` 为根层常量）；俯仰注入点在 `Attitude Control` wrapper 内 `InputConditioning` 输出 2 与内层 `roll_pitch` 输入之间，采用算术融合 `y = orig*(1-e) + m0b*e`，`e=0` 时逐位复现原信号；roll/yaw/thrust 通路零改动。
3. [x] 限制：`|pitch_cmd| ≤ 0.40`（θ_des ≤ 0.209 rad）、积分 ≤ 0.15、变化率 0.25/s；约束位 3（姿态）/位 5（速度失跟）阈值升级为运行时常量 `M0B Att Tol`（默认 0.523 rad）/`M0B Speed Tol`（默认 1.0 m/s），用普通逻辑方块覆盖原 M0-A chart 输出（不触碰 chart 脚本）。
4. [x] 安全监视器：硬标志 [1,2,3,4,6,7] 触发 → 冻结 0.5 s（状态 3，保持最后参考）→ 回退手动参考（状态 4），标志清除 1.5 s 后重新进入；位 5 仅记录不参与门控。
5. [x] 固定速度点试验（`run_air_m0b_tests`）：5/9 m/s 稳态与 6→9 m/s 阶跃，PWM 始终 1500±4 无饱和、姿态在自身限幅内、硬标志静默；安全演示（运行时收紧姿态阈值至 0.15 rad）记录 8 次 `frozen/fallback` 转换并回退至 5 m/s。

验收：**2026-08-31 判为通过，2026-09-01 独立复核后修订为不通过**（`docs/evidence/M0B_REVIEW_20260901.md`）。复核确认的缺陷与缺口：

- **P1（阻塞）**：安装脚本重定向 flags 消费者时误删全部目标连线，`M0A Constraint Flags` 出 1 → `M0B Flags Override` 输入 1（flags_raw）在已保存的 `air_spare.slx`/`air_m0b.slx` 中悬空；透传位 1/2/4/6/7（PWM/RPM 边界、偏航率、功率、NaN）恒 0，故障注入证明"原始位触发→下游位触发→保护动作"链路断裂，原"硬标志静默"结论空洞。
- **P2**：`run_air_m0a_baseline_compare` 把 6DOF 输出 4（DCM）误标为 `Ve`（复核者已用真输出 1 补证差 0，结论未变但脚本须修正并加维度断言）；`run_air_m0b_tests` 阶跃前窗口 `tb>=5.5 & tb<4` 恒空（pre 指标 NaN）、注释阈值 0.5 与代码 2.0 不一致、5 m/s 场景不检查误差；安全演示场景从未到达 active（8 次转换均为 warmup/frozen/fallback 往返，fallback 实为向 5 m/s 上爬）；稳定窗口口径未建立（扰动场景 6–10 s 失跟占比 60–78%）。

**修复与重新验收（2026-09-01）**：安装器重定向改为按目标逐支路删除并新增保存后重载验证（67 条连线断言）；`Ve` 修正为 6DOF 输出 1 并加维度断言（旁路比对差 0）；验收改为名义（roll 正弦置 0：9 m/s 均值误差 0.032 m/s、失跟 0%）/扰动（基线正弦：5/9 m/s 均值 1.615/1.598 m/s，与复核复跑一致）双口径；逐位故障注入（位 1/4/6/7，active 先行）全部"触发→frozen≤0.1 s→fallback≤1 s→（位 6）清除后恢复"。稳定快照 `air_m0b.slx` 已替换。证据：`docs/evidence/M0B_RERUN_20260901.md`。

已知边界（记录，不算缺陷）：模型 `InputConditioning` 忽略 RC 输入，roll 指令由内部正弦（±0.4 @1 rad/s）生成且按约束保留，造成 |Ve_y| 在 0–8.4 m/s 摆动；速度外环只操纵俯仰通道，因此 |v| 跟踪误差受该扰动下限约束（9 m/s 时均值约 1.6 m/s，增益扫描 0.06–0.30 证实提高增益反而放大振荡）。垂直方向为原始模型固有自由落体特性。

### M0-C：速度在线 ESC（`eta = 1`，完成）

目的：验证单变量速度在线功率优化的闭环，而不引入同轴转速比变化。

1. 将算法接口限制为仅输出小范围 `v_ref`；固定 `eta_ref=1`。
2. 采用稳定窗口计算评价值，避免将起飞、瞬态或明显失跟阶段写入成本函数。
3. 将 Git 算法模块封装为可替换的 MATLAB/Simulink 接口；不得向算法暴露模型内部最优值或直接传递原始 PWM 控制权。
4. 运行至少三组不同初始速度/初始参考的无噪声试验；每组均与固定参考基线配对。
5. 输出速度误差、平均/积分功率、能耗差、收敛时间、约束触发次数与 8 路执行器变化。

验收：**已通过（2026-09-01）**。三组名义试验均收敛且无持续饱和/姿态失稳，固定/ESC 配对与确定性复现成立。当前平坦功率面只支持“闭环机制和安全接口成立、无可宣称节能改善”。

### M1：扰动、噪声与时延鲁棒性（完成）

目的：检验 M0 不是理想条件偶然结果。

1. 注入 PDF 路线规定的功率噪声、反馈时延，以及必要的速度测量噪声/风扰动。
2. 固定随机种子并进行多次重复；比较无优化、M0 优化和回退行为。
3. 必要时调整窗口长度、扰动幅值、滤波和冻结条件，但不以牺牲安全约束换取功率数值。

执行基线：`docs/interfaces/M1_ROBUSTNESS.md`（场景矩阵、注入口径、判据与结果回填）。

验收：**已通过（2026-09-01）**。2% 功率噪声、0.5 s 时延及组合扰动下 27 场景零安全误触发，11 组配对 regret 最大 |0.000133%|（门槛 3%），DL 确定性差 0，噪声背景故障回归 4/4；零 `.slx` 变更。regret 门槛满足源于平坦功率面，只证明测量退化未使机制变坏，不构成节能证据。噪声/组合使 ESC 收敛自 4 s 放慢至 8–20 s（如实记录）。

### M2：上下桨转速比 ESC

目的：将 Git 的 `ratio_esc` 从代理目标接入实际 X8 仿真接口。

1. 明确八个电机的上下桨配对、旋向及 `eta` 的一致定义。
2. 新建受约束的 `X8 Control Allocator`：输入总推力/三轴力矩与 `eta_ref`，输出 `omega_1...omega_8`，再映射至 PWM。
3. 保持总推力和关键力矩可控，并检查上下桨比值变化不引入不可接受的偏航力矩或电机饱和。
4. 将 `ratio_esc` 的代理代价替换为平台输出的 `P_est` 或校准后的 `P_e`；保留算法单元测试。
5. 对 `eta` 初值 0.8、1.0、1.2 等场景进行配对基线试验。

验收：所有试验稳定，`eta` 有界且可追踪；姿态/偏航/电机约束满足；结果不差于对应的固定 `eta=1` 或单变量速度优化基线。

**已通过（2026-09-01 原始验收；同日复验问题修复后经 3 会话复验恢复放行）**。执行基线 `docs/interfaces/M2_ETA_ALLOCATOR.md`（八电机配对与 eta 定义、受约束分配器、esc 接线、判据与结果回填，含 §7 协议预注册修订），证据 `docs/evidence/M2_ETA_20260901.md`（原始轮）与 `docs/evidence/M2_REACCEPT_FIX_20260901.md`（复验修复轮）。要点：PWM 域后置分配器在 `Attitude Control` 出 2 分接，η=1 恒等快速路径保证旁路差 0；eta 经全局量交接（跨率信号线进 pwm 路径会破坏位精确旁路，E1 实验记录在案）；修订协议下配对能量门槛 S1/S2/S3 = −0.260%/−0.292%/−0.226%（[90,120] s 收敛末窗，≤ +0.5%，[20,30] 连续性值如实保留）；功率面实测 η=0.8/1.2 比 η=1 高 +1.59%/+0.98%（模型估算口径）；esc 中心 120 s 收敛到 1.005/0.9998/1.0302；单元测试会话隔离、验收硬门槛全覆盖（复验 F2/F4 关闭）。快照 `air_m2.slx`。

### P0--P4：Plane 仿真物理建模（新增并行工作线）

目的：把目前分散在速度、风场、转速比和 PX4 模块中的代理对象统一成一个因果、可替换、以后能用真实数据校准的 Plane 组件。该工作与 M3 方案设计并行，不等待 M3 完成。

建议负责人：霍奕茗（待组内确认）。叶安负责给出 Control 侧接口并审核接入，不要求两人同时编辑同一 `.slx`。

| 工作包 | 内容 | 可独立交付 |
|---|---|---|
| P0 接口与基线 | 固定坐标系、单位、时间戳、有效性、参数来源；复现无风速度功率基线 | 接口字典、默认配置、基线脚本 |
| P1 风与运动学 | 接入 `wind_ne`，计算地速/空速；直线和固定半径圆周轨迹，输出相位、切向量和径向误差 | 独立运动学对象与零风/顺逆风测试 |
| P2 执行与动力学 | 为 `v_ref`、`eta_ref` 增加有限响应、变化率和饱和；圆周场景显式计算 `a_c=v^2/R` 及姿态需求 | 阶跃、边界和圆周需求测试 |
| P3 联合功率对象 | 建立 `P(v_air, eta; theta)` 代理并积分能量；与现有速度/转速比单变量切片对齐 | 功率对象、参数表、切片与敏感性图 |
| P4 电池与适配器 | 增加简化电压/SOC，统一 `reset/step` 适配器、日志和约束标志；保留日后台架数据校准入口 | 可由 ESC、解析调度、RL 和 Harness 共用的 Plane API |

建议最小接口：

```text
输入：t, dt, v_ref, eta_ref, wind_ne[2], path_command, mass, battery_config
输出：v_ground_ne[2], v_air_ne[2], eta_actual, attitude, yaw_rate,
      motor_pwm[8], motor_rpm[8], P_e, E_e, soc, path_phase,
      radial_error, constraint_flags, power_source
```

验收门槛：

1. 零风时空速等于地速；顺风、逆风的符号和空速变化方向正确。
2. `v_ref` 和 `eta_ref` 不发生瞬时跳变，且变化率、边界和饱和标志可复现。
3. 圆周速度提高时 `v^2/R` 与所需倾角方向正确；固定半径一周后相位闭合。
4. `P(v_air, eta)` 的速度切片与转速比切片能退化到现有代理基线；`E_e` 与 `P_e` 数值积分一致。
5. 所有输出带单位、时间戳/有效性和来源；优化器不能读取完整曲面、真实最优点或未来风。
6. 在未标定阶段只报告代理模型结果；不宣称真实 X8 节能、续航或安全结论。

当前交付：`models/plane` 与 `models/px4_x8/+x8phys` 的独立 MATLAB 代理及适配器已实现；P0 静推分档拟合门槛和 P1--P4 对象/接口契约测试已通过。`models/plane` 按接口字典 0.3 将 PathCommand、WindSample 和 ControlCommand 分离，物理对象只从 WindSample 读取真风。`+x8phys` 使用 PAW 40x13.1R 静推数据的 `T=0.08643*n^2`、`P_e=0.34467*n+0.0289367*n^3`、`KV=76.64 rpm/(V*throttle)` 拟合，以及 ZE12070117A-024Ah 电芯的 OCV/容量/内阻量级。静推数据只覆盖空速 0 且约 225--1140 rpm，7S1P、电机动态、非零空速桨效应和整机阻力仍是可覆盖的 `proxy`，不能据此作真实节能或飞行安全结论。`run_x8phys_acceptance`、`run_plane_acceptance` 已通过；整机 S/P 确认、动态校准和接入同一 PX4 `.slx`/Harness/MeasurementAdapter 仍待完成。

### M3：速度与转速比交替协同优化

目的：在不发生两个慢层控制器相互干扰的前提下协同优化 `v_ref` 与 `eta_ref`。M3 方案和调度器可以先在现有代理对象上开发；最终验收须在通过 P0--P4 的同一 Plane 对象上复跑。

1. 规定两个 ESC 的更新时间、保持时间和优先级；一次仅允许一个变量进入搜索，另一个保持。
2. 使用同一稳定窗口与约束总线；记录每次更新的决策、评价样本和冻结原因。
3. 与 M0、M2 两个单变量基线做相同场景比较。

验收：不出现持续振荡或频繁互相覆盖；能耗与跟踪表现不差于两个单变量基线。

执行基线 `docs/interfaces/M3_V_ETA_COORDINATION.md`（v0.2，含 2026-09-04 勘误块）：状态转移、门控、三套参数、执行证据与配对协议合同已冻结；修复方复验与独立验收分别留档，不互相替代。

**2026-09-04 第二轮独立验收：M3 暂不整体放行，已测代理机制与历史数值证据保留。** [第一轮独立报告](evidence/M3_REACCEPT_CODEX_20260904.md) 之后，实施方提交了 [第二轮修复记录](evidence/M3_REACCEPT_ROUND2_FIX_20260904.md)。本次 [第二轮独立报告](evidence/M3_REACCEPT_ROUND2_CODEX_20260904.md) 在 `f949b9c` 确认：配置漂移 F1 和能量口径 F5 在问题范围内关闭；F2 候选参考代替中心、F3 扰动姿态漏判、F4 分段证据绑定与恢复/attempt 治理仍为开放 P1。功能 PARTIAL、基础设施 NOT VALIDATED、环境 OPEN LIMITATION、文档 PARTIAL。三套单测、11 行针对性恢复、M0-A/M0-B/边界通过；历史 14 臂及五名义臂真实中心重算通过，但聚合器独立夹具仍有 6 类假通过。本轮未新跑完整正式批次，也未修改 `.slx` 或 ESC 内核。先按独立报告 §6 关闭剩余问题，再按原冻结门槛复跑；统一 Plane/R4 终验继续后置。

**2026-09-04 第三轮修复（修复方复验）：F2/F3/F4/F6 已按独立报告 §6 关闭并在治理链上重跑 14 臂批次。** [第三轮修复报告](evidence/M3_REACCEPT_ROUND3_FIX_20260904.md)：收敛评价以内核重放的可核验搜索中心为输入（保真门拒收一切非自洽证据）并统一 [192,240)；位 3 姿态门全臂硬判；批次治理（manifest/batchId/attempt/入库有界重试驱动）按规则 v1.7 §2.4–2.9 落地，25/25 篡改负向+备选三段布局正控；恢复矩阵补齐为全笛卡尔 28 行。批次（batchId `9b7a6980`，提交 `ec171b5`）经入库驱动执行，五段 attempt 2/1/2/1/2（堆崩溃如实入账），聚合 PASS，数值与第二轮逐位一致。修复方复验不替代独立验收；项目组复验通过后 M3 代理阶段收口，Plane 接入后执行 R4。

### M4：统一 Wind-Plane-Control Harness 与 SITL/日志回放

目的：将可重复仿真扩展为工程化验证流程。

1. 将 Wind 场景、P0--P4 Plane 对象和 Control 平台接入同一 Harness：起飞、悬停、直线巡航、圆周盘旋、速度切换、扰动、边界与故障回退。
2. 建立统一配置、批量运行、结果汇总和绘图脚本；每一场景保存版本信息。
3. 若使用 PX4 SITL/MAVLink，映射统一接口而不是把算法逻辑散落在 QGC 或日志脚本中。
4. 导入真实台架/飞行数据，完成时间对齐与参数辨识，更新功率、电机响应、推力、阻力、电池模型。
5. 用留出的真实数据验证校准效果，再重复 M0–M3 对比。

验收：Harness 可一键复现；模型校准有训练/验证数据区分；SITL 或日志回放的接口与纯 Simulink 接口一致。

本阶段即 §0.6 的 V2（第 3 条：SITL/HIL）与 V3（第 4/5 条：台架/地面数据校准）入口；禁飞期地面物理验证（倒扣模拟升力、绑缚测试）与实飞（V4）按各级放行条件另行排期，不预支结论。

### 后续：RL / TD3 平台接入（仅在 M4 后）

前提：M0–M4 已有可信目标、状态、约束与安全回退。RL 只能在既有接口内替换慢层决策器，并必须与 ESC/固定基线进行统计比较；不能以未校准模型训练结果代替飞行安全结论。

（说明：`modules/speed_rl_residual` 与 `modules/speed_rl_pytorch` 已完成虚拟代理对象上的接口、对拍和候选策略预研，但不属本节所述的平台接入；其结果不得作为任何平台或飞行结论。）

## 6. 下一次实际工作（2026-09-04 更新）

M2 验收自动化已由第七至十轮关闭（源码指纹/dirty 绑定、verdict 硬断言、篡改与 attempt 负向），第十轮 52/52 针对性矩阵 PASS（`docs/evidence/M2_REACCEPT_ROUND10_CODEX_20260903.md`）；R2022b 堆崩溃按环境限制管理。M3 第二轮独立验收的 F2/F3/F4/F6 已由第三轮修复关闭（修复方复验、14 臂批次治理链重跑 PASS），当前阻塞项转为 **M3 项目组独立复验**与统一闭环缺失。按优先级：

1. **M3 独立复验与代理阶段收口**（叶安牵头，项目组/独立会话执行）：第二轮独立验收的 F2/F3/F4/F6 已由第三轮修复关闭（[`evidence/M3_REACCEPT_ROUND3_FIX_20260904.md`](evidence/M3_REACCEPT_ROUND3_FIX_20260904.md)，batchId `9b7a6980`），但修复方复验不替代独立验收——由独立会话按 [`ACCEPTANCE_AUTOMATION_RULES.md`](ACCEPTANCE_AUTOMATION_RULES.md) 复跑 14 臂批次、恢复矩阵与篡改负向，通过后 M3 代理阶段收口。
2. **harness↔Plane 对接（"集成飞机、控制、环境"的第一落点，R1/R2 载体）**（周航正总装，霍奕茗 Plane 侧）：把 `harness` 三模块架构中的 aircraft 代理对象替换为 `models/plane/+plane` 的 `plane.step`（经 P4 适配器），Wind 侧统一走 ENV-A/B 的 `PathCommand`+`WindTruth`；先在 Mock/零风场景跑通接口契约测试（R1），再在同一 Plane 上复跑 fixed / nominal_sched / ESC / qnewton 四模式配对（R2 无风基线）。Plane P0--P4 契约测试已绿，本步是把三条线真正接进同一闭环。
3. **ENV 线统一收口（R3 前置）**（王健祺）：统一 WindTruth NE 符号（关闭 `wind_field_sched` 局部加号适配）、WindMeasurement 采样/噪声/延迟/年龄/缺测（ENV-C）、训练/调参/未见场景与种子清单（ENV-D）；同步补齐任务7 入口脚本（`START_HERE.m`/`run_task7_acceptance.m` 未入库，登记与文件不一致）。
4. **UI-A WPC 统一面板启动**（§0.7 展示层；建议周航正或于跃认领）：先用现有代理对象出样张（任务8/9 面板框架 + `run_mop_moe_demo` 数据通路），harness↔Plane 对接完成后切换真实数据源；UI-B Plane 物理视图随后。
5. **M3 统一 Plane 复跑（R4 放行）**：待第 1、2 条完成后，将 M3 配对批次从代理对象迁到统一 Plane；不差于两个单变量基线、零硬违规才放行 R4。
6. **V2 预研（PX4 SITL/HIL 路线确认）**：确认 SITL 与 HITL 的先后、MAVLink 接口映射与接口字典 0.3 的对应关系，为 M4 第 3 条做准备；只做路线确认，不提前实现。

汇合点与红线不变：Plane 单元通过后由叶安按接口接入 `air_spare.slx`，运行 M0/M2 基线回归和 M3 配对试验；M3 `.slx` 结构变更沿用 M2 教训——新增块一律单输出口、验收入口函数化并遵循 [`ACCEPTANCE_AUTOMATION_RULES.md`](ACCEPTANCE_AUTOMATION_RULES.md)。起步前可顺带清理 `M0B Flags Override/Att Demux` 空支路告警（非阻塞）。

<details><summary>历史：M0-C 交付清单（已完成）</summary>

M0-C 只作用于 `air_spare.slx`，顺序为：

1. 将算法接口限制为仅输出小范围 `v_ref`（在现有 `v_ref ∈ [0,15]` 与变化率 2 m/s² 限制内收紧到搜索带）；固定 `eta_ref=1`。
2. 采用稳定窗口计算评价值：排除 `status ≠ 1/2`（未进入 active/warm-up 完成前）、速度失跟（位 5 置位）阶段；名义场景可得 ≥3 s 干净窗，扰动场景如实报告窗口受限（最长 0.87–1.59 s），窗口长度作为显式配置归档。（2026-09-01 codex 复验意见 4.1 统一口径：成本窗口仅取 status==2 且位 5 静默样本，warmup/参考爬坡（status 1）一律排除，见 `docs/interfaces/M0C_SPEED_ESC.md` §3。）
3. 将 Git 算法模块（`modules/ratio_esc` 的 ESC 内核语义）封装为可替换的 MATLAB/Simulink 接口：输入仅 `t、v、P_e、E_e、attitude、yaw_rate、constraint_flags`，输出仅 `v_ref`；替换 `M0B v Ref Optimizer` 常量占位；不得向算法暴露模型内部最优值或 PWM 控制权。
4. 运行至少三组不同初始速度/初始参考的无噪声试验，每组与固定参考基线配对（复用名义/扰动双口径与 `run_air_m0b_safety_injection` 的保护回归）。
5. 输出速度误差、平均/积分功率、能耗差、收敛时间、约束触发次数与 8 路执行器变化；每次结构变更后重跑 `run_air_m0a_baseline_compare`（已含真 `Ve` 维度断言）。

当前不接入 `eta` 分配器，也不做 RL。

</details>

算法线进展备注（2026-09-01）：`modules/speed_esc`（平飞速度 ESC，虚拟功率代理上完成 74 场景性能验收与 Python 逐样本对齐）与 `modules/speed_rl_residual`（速度基线之上的 TD3 残差修正预研）已并入本仓库。二者均属优化算法线在代理对象上的预研，**不改变本路线的平台线计划**：M0-C 已按 `M0C_SPEED_ESC.md` 采用 `modules/ratio_esc` 内核完成速度语义映射；`modules/speed_esc` 的回归梯度估计器、窗口选择与验收结论可作为后续内核替换与参数整定的候选依据。

M0-C 最终交付边界为：算法接口仅输出小范围 `v_ref`（固定 `eta_ref=1`）、稳定窗口评价排除 warmup/非 active/失跟、封装 Git 算法模块替换 `M0B v Ref Optimizer` 占位、≥3 组初值配对基线试验、每次结构变更后重跑旁路比较。M1 仍不接入 `eta` 分配器，也不做平台侧 RL。

## 7. 变更与结果管理规则

- 验收入口、验收链与全局量状态管理遵循 [`ACCEPTANCE_AUTOMATION_RULES.md`](ACCEPTANCE_AUTOMATION_RULES.md)（入口函数化、全局量注册表与状态协议、错误路径实跑、覆盖矩阵、裕量与措辞纪律）。
- 分层判定、问题分类与关闭三件套的跨项目采用见 [`decisions/ADR-003-layered-acceptance-closure-governance.md`](decisions/ADR-003-layered-acceptance-closure-governance.md)（叶安负责；项目组 2026-09-03 采纳，指导教师复核记录待补）。功率图信息边界见 Proposed [`decisions/ADR-004-power-map-information-boundary.md`](decisions/ADR-004-power-map-information-boundary.md)（原 ADR-003 编号已让渡给分层治理，重命名由周航正 2026-09-03 完成）。

- `air.slx`：保留为已验证原始基线，不直接改。
- `air_spare.slx`：当前开发模型；每一个可验收阶段另存稳定快照，例如 `air_m0a.slx`。
- 每次模型结构变更同时更新接口文档、运行脚本和一条结果说明。
- 仿真生成的 `slprj/`、`*.slxc`、autosave 文件不进入版本控制。
- 原始真实数据只读保存；清洗数据、参数集、运行配置和图表须可追溯到原始数据与代码版本。
